<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shopping.mapper.UserPointMapper">
    
    <select id="selectBalanceByUserId" resultType="java.lang.Integer">
        SELECT balance FROM t_user_point 
        WHERE user_id = #{userId}
    </select>
    
    <update id="increaseBalance">
        INSERT INTO t_user_point (user_id, balance) 
        VALUES (#{userId}, #{amount}) 
        ON DUPLICATE KEY UPDATE 
        balance = balance + #{amount}
    </update>
    
    <update id="decreaseBalance">
        UPDATE t_user_point 
        SET balance = balance - #{amount} 
        WHERE user_id = #{userId} AND balance >= #{amount}
    </update>
    
</mapper>
