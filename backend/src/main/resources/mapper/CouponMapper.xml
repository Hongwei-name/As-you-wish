<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shopping.mapper.CouponMapper">
    
    <select id="selectAvailableCoupons" resultType="com.shopping.entity.Coupon">
        SELECT * FROM t_coupon 
        WHERE status = 1 
        AND remaining_quantity > 0 
        AND start_time &lt;= NOW() 
        AND end_time &gt;= NOW()
    </select>
    
    <update id="decreaseRemainingQuantity">
        UPDATE t_coupon 
        SET remaining_quantity = remaining_quantity - 1 
        WHERE id = #{couponId} AND remaining_quantity > 0
    </update>
    
</mapper>
