<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shopping.mapper.CouponUserMapper">
    
    <select id="selectByUserId" resultType="com.shopping.entity.CouponUser">
        SELECT * FROM t_coupon_user 
        WHERE user_id = #{userId}
        ORDER BY receive_time DESC
    </select>
    
    <select id="countByUserIdAndCouponId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM t_coupon_user 
        WHERE user_id = #{userId} AND coupon_id = #{couponId}
    </select>
    
</mapper>
